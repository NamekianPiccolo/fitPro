{
  "name": "fitvision",
  "version": "1.0.0",
  "description": "fitVision - Real-Time AI Exercise Tracker",
  "scripts": {
    "tauri": "tauri",
    "tauri:dev": "python scripts/build_dist.py && tauri dev",
    "tauri:build": "python scripts/build_dist.py && tauri build",
    "tauri:android": "python scripts/build_dist.py && tauri android build --target aarch64",
    "flask": "python app.py",
    "build-dist": "python scripts/build_dist.py",
    "postinstall": "python scripts/setup_offline.py"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@fontsource/inter": "^5.2.8",
    "@fontsource/outfit": "^5.2.8",
    "@fortawesome/fontawesome-free": "^7.2.0",
    "@mediapipe/camera_utils": "^0.3.1675466862",
    "@mediapipe/pose": "^0.5.1675469404",
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-shell": "^2.3.5"
  }
}
